version: '3.8'

services:

  nodejs-app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    env_file: ./.env
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    command: "npm start"